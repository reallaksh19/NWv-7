--- src/insight/src/cache/cacheManager.ts	2026-04-17 17:30:00.000000000 +0000
+++ src/insight/src/cache/cacheManager.ts	2026-04-17 19:04:19.558191229 +0000
@@ -10,15 +10,6 @@
   InsightConfig,
 } from "../types";

-// ── Slot target times (ms offset from "now") ─────────────────────────────────
-
-const SLOT_TARGET_OFFSET: Record<SnapshotSlot, number> = {
-  now:       0,
-  minus4h:   4  * 60 * 60 * 1000,
-  minus12h:  12 * 60 * 60 * 1000,
-  minus24h:  24 * 60 * 60 * 1000,
-};
-
 // ── In-process store (swap for Redis/DB in production) ───────────────────────

 const store = new Map<SnapshotSlot, SnapshotCacheEntry>();
@@ -42,7 +33,6 @@
   const ageMs   = Date.now() - entry.fetchedAt;
   const ttl     = cfg.CACHE_TTL[slot];
   const tol     = cfg.CACHE_TOLERANCE[slot];
-  const target  = SLOT_TARGET_OFFSET[slot];

   // Expired beyond TTL + tolerance → stale, reject
   if (ageMs > ttl + tol) return null;
