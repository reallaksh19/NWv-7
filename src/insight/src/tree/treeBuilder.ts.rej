--- src/insight/src/tree/treeBuilder.ts	2026-04-17 17:30:00.000000000 +0000
+++ src/insight/src/tree/treeBuilder.ts	2026-04-17 19:04:19.545379539 +0000
@@ -59,10 +59,11 @@
   }, 0);
   const redundancyPenalty = Math.max(0, (maxSim - 0.70) * 2); // penalty starts at 0.70 sim

+  // FIX H-2: weights now sum to 1.0 (was 0.9 — newSourceScore was 0.2, raised to 0.3)
   return Math.max(0,
     0.4 * newFactsScore +
     0.3 * newAngleScore +
-    0.2 * newSourceScore -
+    0.3 * newSourceScore -
     0.1 * redundancyPenalty
   );
 }
